SERVICE=app
ENV=dev
PORT=8000
VERSION=1.0.0

# OIDC / JWT (optional; when set, non-public routes require Authorization: Bearer <token>)
OIDC_ISSUER=
OIDC_AUDIENCE=
# OIDC_JWKS_URL=  # defaults to "${OIDC_ISSUER}/.well-known/jwks.json" when empty
JWKS_CACHE_TTL_SECONDS=300
JWT_CLOCK_SKEW_SECONDS=60
